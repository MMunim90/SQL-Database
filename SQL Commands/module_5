-- UNION 
SELECT *
FROM EMPLOYEES
WHERE SALARY > 10000
UNION
SELECT *
FROM EMPLOYEES
WHERE DEPARTMENT_ID = 100;


-- UNION ALL
SELECT *
FROM EMPLOYEES
WHERE SALARY > 10000
UNION ALL
SELECT *
FROM EMPLOYEES
WHERE DEPARTMENT_ID = 100;


-- INTERSECT 
SELECT *
FROM EMPLOYEES
WHERE SALARY > 10000
INTERSECT  -- don't worry about this error. the query is steal running. and the syntax is correct.  
SELECT *
FROM EMPLOYEES
WHERE DEPARTMENT_ID = 100;



SELECT *
FROM EMPLOYEES
WHERE SALARY > 10000 AND DEPARTMENT_ID != 100